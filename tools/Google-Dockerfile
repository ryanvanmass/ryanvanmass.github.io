FROM ryanvanmass/jekyll-site:latest as build

RUN apt update
RUN apt install git -y

RUN git clone https://github.com/ryanvanmass/ryanvanmass.github.io /mnt

WORKDIR /mnt

ENTRYPOINT [ "bundle", "exec", "jekyll", "serve", "-H", "0.0.0.0", "-P", "4000", "-D" ]
